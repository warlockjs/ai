// Main API
export { fallbackModel } from "./fallback-model";

// Types — the fallback-model contract surface.
export type {
  FallbackAttempt,
  FallbackModelContract,
  FallbackModelOptions,
  FallbackRetryPredicate,
} from "../contracts/fallback-model.contract";
